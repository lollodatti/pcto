#include <stdio.h>

int main()
{
    int a;
    int b;
    int c;
    int d;
    int e;
    int f;
    int g;
    

    printf("inserisci un numero\n");
    scanf("%d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g);
    printf("media %d\n", (a + b + c + d + e + f) / g);
    
}